# Don't install package versions published within the last 7 days.
# Mitigates supply-chain attacks that rely on rapid consumption of a
# freshly-published malicious version before it's detected/yanked.
# Requires npm >= 11.10.0 (enforced at install time, measured in days).
min-release-age=7
